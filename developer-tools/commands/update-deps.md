---
description: Update project dependencies safely
argument-hint: "[package name or 'all']"
---

Load the `dependency-updater` skill and update dependencies.

Auto-detect the project type and apply safe updates automatically, prompting for major version bumps.
