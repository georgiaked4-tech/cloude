---
description: Convert a webpage to clean Markdown
argument-hint: "[URL]"
---

Load the `web-to-markdown` skill and convert the webpage.

Uses a local Puppeteer + Readability CLI, suitable for JS-rendered pages.
