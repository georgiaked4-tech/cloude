---
description: Create a new Claude Code plugin
argument-hint: "[plugin name and description]"
---

Load the `plugin-forge` skill and create the plugin structure.

Generates the plugin directory, plugin.json manifest, and registers it in marketplace.json.
