---
description: Generate C4 architecture diagrams using Mermaid
argument-hint: "[system or component name]"
---

Load the `c4-architecture` skill and generate C4 diagrams.

If a system name is provided, use it. Otherwise ask what to diagram (context, container, component, or deployment level).
